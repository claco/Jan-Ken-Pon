<%=h flash[:notice]%>

<% if !authenticated? %>	
	<%=error_messages_for 'user'%>
	<% form_for :User do |f| -%>
		<%= f.label :email, "Email Address" %>
		<%= f.text_field :email %>

		<%= f.submit "Reset" %>
	<% end -%>
<% end %>